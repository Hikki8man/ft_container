#include "vector.hpp"
#include "iterator_traits.hpp"
#include <iostream>
#include <vector>
#include <stack>


int main(void) {
	// {
	// 	std::vector<int> v;

	// 	// std::iterator::
	// 	v.reserve(10);
	// 	v.resize(7, 8);
	// 	v.push_back(2);
	// 	v.shrink_to_fit();

	// 	// std::vector<int>::iterator a = v.begin();

	// 	// std::cout << a-> << std::endl;

	// 	std::cout << "v.size() = " << v.size() << std::endl;
	// 	std::cout << "v.capacity() = " << v.capacity() << std::endl;
	// 	std::cout << std::boolalpha << "v.empty() = " << v.empty() << std::endl;
	// 	for (size_t i = 0; i < v.size(); ++i)
	// 		std::cout << v[i] << std::endl;

	// 	ft::vector<int> a;
	// 	ft::vector_iterator<ft::vector<int> > it;
	
	// }

	// std::cout << "---------------------------------------------" << std::endl;

	

	// {
	// 	vector<int> v;
	// 	v.reserve(10);
	// 	v.resize(7, 8);
	// 	v.push_back(2);
	// 	v.shrink_to_fit();
	// 	std::cout << "v.size() = " << v.size() << std::endl;
	// 	std::cout << "v.capacity() = " << v.capacity() << std::endl;
	// 	std::cout << std::boolalpha << "v.empty() = " << v.empty() << std::endl;
	// 	for (size_t i = 0; i < v.size(); ++i)
	// 		std::cout << v[i] << std::endl;
	// }

	std::cout << "---------------------------------------------" << std::endl;

	std::stack<int> a;
}